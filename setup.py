from setuptools import setup
setup(
    name='webscrape-dipson94',
    version='0.0.1',
    author='Dipson',
    description='A webscrape tool',
    packages=['webscrape'],
    install_requires=[
        'bs4','requests'],
)
